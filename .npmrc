# 设置默认安装源为淘宝镜像，加快依赖下载速度
registry=https://registry.npmmirror.com

# 为特定作用域包指定私有源（适用于公司内部模块）
# @myscope:registry=https://registry.my.com/

# 禁用包安装过程中执行的脚本，提高安装安全性
# ignore-scripts=true

# 开启严格版本控制，确保安装的依赖版本与 package.json 中定义的完全一致
# save-exact=true

# 设置代理服务器（适用于受限网络环境）
# proxy=http://proxy.example.com/
# https-proxy=https://proxy.example.com/

# 定义日志级别（silent, warn, info, verbose, silly）
# loglevel=warn

# 设置 npm 命令的超时时间，单位为毫秒
# fetch-timeout=60000

# 使用身份验证令牌访问私有仓库（避免手动输入凭证）
# //registry.example.com/:_authToken=your-auth-token

# 如果所有请求都需要认证，则启用 always-auth
# always-auth=true

# 自定义缓存目录位置（例如在企业内网中使用更快的本地缓存）
# cache=/path/to/custom/npm-cache

# 设置 Node-SASS 的二进制文件镜像源
# sass_binary_site=https://registry.npmmirror.com/-/binary/node-sass

# 自定义用户代理字符串（用于特定环境追踪下载来源）
# user-agent=my-custom-npm-client/1.0

# 限制最大并行下载数，适用于网络受限的情况
# maxsockets=3
